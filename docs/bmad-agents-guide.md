# BMAD Agent 对话指南

**项目**: travis-blog  
**最后更新**: 2025-11-15

---

## 📖 概述

BMAD (Build Method for AI Development) 定义了一系列专业 Agent，每个 Agent 都有特定的角色、专业知识和菜单系统。本文档说明如何与这些 Agent 进行对话。

---

## 🎭 可用的 Agent

### BMM (Build Method for Modern) Agents

| Agent | 名称 | 角色 | 图标 | 主要职责 |
|-------|------|------|------|----------|
| **analyst** | Mary | 业务分析师 | 📊 | 需求分析、市场研究、产品简介 |
| **pm** | John | 产品经理 | 📋 | 产品需求文档、Epic/Story 分解、PRD 验证 |
| **architect** | Winston | 系统架构师 | 🏗️ | 架构设计、技术规范、解决方案门检查 |
| **dev** | Amelia | 开发工程师 | 💻 | 故事实现、代码开发、测试 |
| **sm** | Bob | Scrum Master | 🏃 | 故事创建、Sprint 规划、工作流管理 |
| **ux-designer** | - | UX 设计师 | 🎨 | UX 设计、用户界面设计 |
| **tech-writer** | - | 技术文档编写者 | 📝 | 技术文档编写 |
| **tea** | - | TEA Agent | ☕ | TEA 相关功能（测试、评估、分析） |

### CORE Agents

| Agent | 名称 | 角色 | 图标 | 主要职责 |
|-------|------|------|------|----------|
| **bmad-master** | BMad Master | 主执行器 | 🧙 | 任务执行、工作流编排、资源管理 |

---

## 🚀 如何激活 Agent

### 方法 1: 使用 Cursor Rules 引用（推荐）

在对话中引用 Agent 规则文件：

```
@bmad/bmm/agents/pm
```

或使用完整路径：

```
@.cursor/rules/bmad/bmm/agents/pm.mdc
```

### 方法 2: 直接使用工作流指令

许多工作流会自动加载相应的 Agent。例如：

```
/bmad:bmm:workflows:create-story
```

这会自动使用 `sm` (Scrum Master) agent。

### 方法 3: 在新对话中明确指定

在新对话开始时，明确说明要使用的 Agent：

```
我想和 Product Manager (pm) agent 对话
```

或：

```
请加载 @bmad/bmm/agents/pm
```

---

## 💬 Agent 对话流程

### 1. Agent 激活

当 Agent 被激活时，它会：

1. **加载配置文件**: 自动加载 `.bmad/bmm/config.yaml` 获取用户信息
2. **显示问候**: 使用配置中的 `user_name` 问候用户
3. **显示菜单**: 显示所有可用的菜单项（编号列表）
4. **等待输入**: 停止并等待用户选择

### 2. 菜单系统

每个 Agent 都有一个菜单系统，包含：

- **编号菜单项**: 使用数字选择（如 `1`, `2`, `3`）
- **命令触发器**: 使用 `*` 前缀的命令（如 `*help`, `*create-story`）
- **模糊匹配**: 支持文本匹配（不区分大小写）

### 3. 输入方式

你可以通过以下方式与 Agent 交互：

- **数字**: 直接输入菜单项编号（如 `1`）
- **命令**: 输入命令触发器（如 `*help`）
- **文本**: 输入菜单项的部分文本（如 `create story`）

---

## 📋 各 Agent 详细说明

### 📊 Analyst (Mary) - 业务分析师

**角色**: 战略业务分析师 + 需求专家  
**专长**: 市场研究、竞争分析、需求获取

**主要菜单项**:
- `*workflow-init` - 启动新的工作流路径
- `*workflow-status` - 检查工作流状态
- `*brainstorm-project` - 项目头脑风暴
- `*product-brief` - 生成产品简介
- `*research` - 领域研究
- `*document-project` - 生成项目文档
- `*party-mode` - 与其他 Agent 群聊
- `*adv-elicit` - 高级需求获取技术

**使用场景**:
- 项目初始阶段的需求分析
- 市场研究和竞争分析
- 产品简介创建
- 项目文档生成

**示例对话**:
```
你: @bmad/bmm/agents/analyst
Mary: 你好 Travis！我是 Mary，业务分析师。我可以帮助你进行需求分析、市场研究和产品规划。

1. *workflow-init - 启动新的工作流路径
2. *workflow-status - 检查工作流状态
3. *brainstorm-project - 项目头脑风暴
...

你: 1
Mary: [执行 workflow-init 工作流]
```

---

### 📋 PM (John) - 产品经理

**角色**: 调查性产品策略师 + 市场敏锐的 PM  
**专长**: 产品需求文档、Epic/Story 分解、PRD 验证

**主要菜单项**:
- `*workflow-init` - 启动新的工作流路径
- `*workflow-status` - 检查工作流状态
- `*create-prd` - 创建产品需求文档
- `*create-epics-and-stories` - 将 PRD 需求分解为可实施的 Epic 和 Story
- `*validate-prd` - 验证 PRD + Epic + Story 的完整性和质量
- `*tech-spec` - 创建技术规范（Level 0-1 项目）
- `*validate-tech-spec` - 验证技术规范文档
- `*correct-course` - 课程纠正分析
- `*party-mode` - 与其他 Agent 群聊

**使用场景**:
- 创建和验证 PRD
- 将需求分解为 Epic 和 Story
- 产品规划和质量验证

**示例对话**:
```
你: @bmad/bmm/agents/pm
John: 你好 Travis！我是 John，产品经理。我可以帮助你创建 PRD、分解 Epic 和 Story。

1. *help - 显示编号菜单
2. *workflow-init - 启动新的工作流路径
3. *create-prd - 创建产品需求文档
...

你: *create-epics-and-stories
John: [执行 create-epics-and-stories 工作流]
```

---

### 🏗️ Architect (Winston) - 系统架构师

**角色**: 系统架构师 + 技术设计领导者  
**专长**: 分布式系统、云基础设施、API 设计

**主要菜单项**:
- `*workflow-status` - 检查工作流状态
- `*create-architecture` - 创建可扩展架构
- `*validate-architecture` - 验证架构文档
- `*solutioning-gate-check` - 验证解决方案完成，准备进入 Phase 4
- `*party-mode` - 与其他 Agent 群聊

**使用场景**:
- 架构设计和文档创建
- 技术规范制定
- 架构验证和门检查

**示例对话**:
```
你: @bmad/bmm/agents/architect
Winston: 你好 Travis！我是 Winston，系统架构师。我可以帮助你设计系统架构和技术规范。

1. *help - 显示编号菜单
2. *workflow-status - 检查工作流状态
3. *create-architecture - 创建可扩展架构
...

你: 3
Winston: [执行 create-architecture 工作流]
```

---

### 💻 Dev (Amelia) - 开发工程师

**角色**: 高级实施工程师  
**专长**: 故事实现、代码开发、测试

**重要提示**: Dev Agent 在开始实现之前需要：
1. Story 文件已加载且状态为 "Approved"
2. Story Context XML 文件已生成

**主要菜单项**:
- `*help` - 显示编号菜单
- `*workflow-status` - 检查工作流状态
- `*develop-story` - 执行 Dev Story 工作流，实现任务和测试
- `*story-done` - 在 DoD 完成后标记故事完成
- `*code-review` - 对标记为 Ready for Review 的故事进行代码审查

**使用场景**:
- 实现已批准的故事
- 编写代码和测试
- 代码审查

**示例对话**:
```
你: @bmad/bmm/agents/dev
Amelia: 你好 Travis！我是 Amelia，开发工程师。我可以帮助你实现故事和编写代码。

1. *help - 显示编号菜单
2. *workflow-status - 检查工作流状态
3. *develop-story - 执行 Dev Story 工作流
...

你: *develop-story
Amelia: [检查 Story Context，然后开始实现]
```

---

### 🏃 SM (Bob) - Scrum Master

**角色**: 技术 Scrum Master + 故事准备专家  
**专长**: Agile 仪式、故事准备、创建清晰的用户故事

**主要菜单项**:
- `*help` - 显示编号菜单
- `*workflow-status` - 检查工作流状态
- `*sprint-planning` - 从 Epic 文件生成或更新 sprint-status.yaml
- `*epic-tech-context` - 创建 Epic 技术规范（可选）
- `*create-story` - 创建草稿故事
- `*story-context` - 组装动态 Story Context (XML)
- `*story-ready-for-dev` - 标记故事准备就绪（不生成 Story Context）
- `*epic-retrospective` - 在 Epic 完成后促进团队回顾
- `*correct-course` - 执行课程纠正任务
- `*party-mode` - 与其他 Agent 群聊

**使用场景**:
- 创建和准备用户故事
- Sprint 规划
- Epic 技术上下文创建
- 团队回顾

**示例对话**:
```
你: @bmad/bmm/agents/sm
Bob: 你好 Travis！我是 Bob，Scrum Master。我可以帮助你创建故事、规划 Sprint。

1. *help - 显示编号菜单
2. *workflow-status - 检查工作流状态
3. *sprint-planning - Sprint 规划
4. *create-story - 创建草稿故事
...

你: *create-story
Bob: [执行 create-story 工作流]
```

---

### 🎨 UX Designer (Sally) - UX 设计师

**角色**: 用户体验设计师 + UI 专家  
**专长**: 用户研究、交互设计、AI 辅助工具

**主要菜单项**:
- `*workflow-status` - 检查工作流状态
- `*create-design` - 进行设计思维工作坊，定义用户规范
- `*validate-design` - 验证 UX 规范和设计工件
- `*party-mode` - 与其他 Agent 群聊

**使用场景**:
- 创建用户体验设计
- 用户界面设计
- UX 规范验证

**示例对话**:
```
你: @bmad/bmm/agents/ux-designer
Sally: 你好 Travis！我是 Sally，UX 设计师。我可以帮助你创建用户体验设计。

1. *help - 显示编号菜单
2. *workflow-status - 检查工作流状态
3. *create-design - 进行设计思维工作坊
...

你: 3
Sally: [执行 create-design 工作流]
```

---

### 📚 Tech Writer (Paige) - 技术文档编写者

**角色**: 技术文档专家 + 知识管理者  
**专长**: CommonMark、DITA、OpenAPI、技术文档编写

**主要菜单项**:
- `*document-project` - 全面项目文档（现有项目分析、架构扫描）
- `*create-api-docs` - 创建 API 文档（OpenAPI/Swagger 标准）
- `*create-architecture-docs` - 创建架构文档（包含图表和 ADR）
- `*create-user-guide` - 创建面向用户的指南和教程
- `*audit-docs` - 审查文档质量并提出改进建议
- `*generate-diagram` - 生成 Mermaid 图表（架构、序列、流程、ER、类、状态）
- `*validate-doc` - 根据标准和最佳实践验证文档
- `*improve-readme` - 审查和改进 README 文件
- `*explain-concept` - 创建清晰的技术解释（包含示例和图表）
- `*standards-guide` - 显示 BMAD 文档标准参考

**使用场景**:
- 编写技术文档
- 文档维护
- 文档质量审查
- 生成技术图表

**示例对话**:
```
你: @bmad/bmm/agents/tech-writer
Paige: 你好 Travis！我是 Paige，技术文档编写者。我可以帮助你编写和维护技术文档。

1. *help - 显示编号菜单
2. *document-project - 全面项目文档
3. *generate-diagram - 生成 Mermaid 图表
...

你: *generate-diagram
Paige: [询问图表类型和内容，然后生成 Mermaid 图表]
```

---

### ☕ TEA Agent - 测试评估分析

**角色**: TEA Agent  
**专长**: 测试、评估、分析

**使用场景**:
- 测试相关任务
- 评估和分析

---

### 🧙 BMad Master - 主执行器

**角色**: 主任务执行器 + BMad 专家 + 引导协调者  
**专长**: BMAD Core 平台、任务执行、工作流编排

**主要菜单项**:
- `*help` - 显示编号菜单
- `*list-tasks` - 列出可用任务
- `*list-workflows` - 列出工作流
- `*party-mode` - 与所有 Agent 群聊

**使用场景**:
- 查看可用任务和工作流
- 执行通用任务
- 与其他 Agent 群聊

---

## 🎯 常用对话模式

### 模式 1: 直接使用工作流指令

最简单的方式是直接使用工作流指令，系统会自动加载相应的 Agent：

```
/bmad:bmm:workflows:create-story
```

这会自动使用 `sm` (Scrum Master) agent。

### 模式 2: 激活 Agent 后使用菜单

1. 激活 Agent:
   ```
   @bmad/bmm/agents/pm
   ```

2. Agent 显示菜单后，选择菜单项:
   ```
   你: 3
   或
   你: *create-prd
   ```

3. Agent 执行相应的工作流或任务

### 模式 3: 使用 Party Mode 与多个 Agent 对话

使用 `*party-mode` 可以与多个 Agent 同时对话：

```
你: @bmad/bmm/agents/pm
John: [显示菜单]
你: *party-mode
[多个 Agent 同时参与对话]
```

---

## 📝 Agent 交互规则

### 通用规则

1. **语言**: Agent 会根据 `.bmad/bmm/config.yaml` 中的 `communication_language` 设置使用相应语言
2. **用户名称**: Agent 会使用配置中的 `user_name` 称呼你
3. **角色扮演**: Agent 会完全进入角色，保持角色特征直到退出
4. **菜单系统**: 所有菜单项使用 `*` 前缀，不是 markdown 格式
5. **编号列表**: 菜单使用数字编号，子选项使用字母

### 输入匹配规则

- **数字输入**: 直接匹配菜单项编号
- **命令输入**: 匹配 `*` 前缀的命令（如 `*help`）
- **文本输入**: 不区分大小写的子字符串匹配
- **多个匹配**: 如果多个菜单项匹配，Agent 会要求澄清
- **无匹配**: 如果无匹配，Agent 会显示 "Not recognized"

### 工作流执行规则

当菜单项有 `workflow` 属性时：

1. Agent 会加载 `.bmad/core/tasks/workflow.xml`
2. 读取完整的工作流配置文件
3. 按照工作流步骤精确执行
4. 每完成一个步骤就保存输出（不批量保存）

---

## 🔧 配置要求

### 必需配置文件

Agent 需要以下配置文件才能正常工作：

**`.bmad/bmm/config.yaml`**:
```yaml
user_name: "Travis"
communication_language: "中文"
output_folder: "docs"
# ... 其他配置
```

如果配置文件缺失或无法加载，Agent 会停止并报告错误。

---

## 💡 最佳实践

### 1. 明确指定 Agent

在开始对话时，明确指定要使用的 Agent：

```
我想和 Product Manager agent 对话
```

或：

```
@bmad/bmm/agents/pm
```

### 2. 使用工作流指令（推荐）

对于常见任务，直接使用工作流指令更高效：

```
/bmad:bmm:workflows:create-story
```

### 3. 查看工作流状态

如果不确定下一步该做什么，先查看工作流状态：

```
/bmad:bmm:workflows:workflow-status
```

或：

```
@bmad/bmm/agents/sm
[然后选择 *workflow-status]
```

### 4. 使用 Party Mode 进行复杂讨论

对于需要多个视角的复杂问题，使用 Party Mode：

```
@bmad/bmm/agents/pm
[选择 *party-mode]
```

### 5. 退出 Agent

完成对话后，使用 `*exit` 退出：

```
你: *exit
Agent: [确认退出]
```

---

## 🎯 快速参考

### 按任务类型选择 Agent

| 任务类型 | 推荐 Agent | 工作流指令 |
|---------|-----------|-----------|
| 需求分析 | analyst | `/bmad:bmm:workflows:research` |
| 创建 PRD | pm | `/bmad:bmm:workflows:prd` |
| 架构设计 | architect | `/bmad:bmm:workflows:architecture` |
| 创建故事 | sm | `/bmad:bmm:workflows:create-story` |
| 实现故事 | dev | `/bmad:bmm:workflows:dev-story` |
| 代码审查 | sm | `/bmad:bmm:workflows:code-review` |
| Sprint 规划 | sm | `/bmad:bmm:workflows:sprint-planning` |
| Epic 回顾 | sm | `/bmad:bmm:workflows:retrospective` |

### 常用命令速查

| 命令 | Agent | 说明 |
|------|--------|------|
| `*help` | 所有 | 显示菜单 |
| `*workflow-status` | 所有 | 查看工作流状态 |
| `*workflow-init` | analyst, pm | 启动新工作流 |
| `*party-mode` | 所有 | 与其他 Agent 群聊 |
| `*exit` | 所有 | 退出 Agent |

---

## 📚 相关文档

- [BMad 标准指令参考](./bmad-standard-commands.md)
- [工作流状态报告](./workflow-status-report-current.md)
- [BMad 工作流文档](../.bmad/bmm/workflows/)

---

## 🔗 Agent 文件位置

所有 Agent 定义文件位于：

- **BMM Agents**: `.cursor/rules/bmad/bmm/agents/` 或 `.bmad/bmm/agents/`
- **CORE Agents**: `.cursor/rules/bmad/core/agents/` 或 `.bmad/core/agents/`

---

## ❓ 常见问题

### Q: 如何知道该使用哪个 Agent？

A: 查看 [BMad 标准指令参考](./bmad-standard-commands.md)，每个工作流都标注了对应的 Agent。

### Q: 可以直接使用工作流指令而不激活 Agent 吗？

A: 可以！直接使用工作流指令（如 `/bmad:bmm:workflows:create-story`）会自动加载相应的 Agent。

### Q: Agent 会记住之前的对话吗？

A: Agent 在激活时会加载配置文件，但不会自动记住之前的对话。如果需要上下文，可以在新对话中提供。

### Q: 如何退出 Agent？

A: 使用 `*exit` 命令退出 Agent。

### Q: 可以同时与多个 Agent 对话吗？

A: 可以！使用 `*party-mode` 可以与多个 Agent 同时对话。

---

_最后更新：2025-11-15_

